name: Trigger 3d Chinese Production Build

on:
  push:
    branches: [ production ]
    paths: [ 'chinese/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-3d-chinese-production
